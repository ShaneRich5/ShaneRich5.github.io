{
  "name": "shane-s-blog",
  "version": "1.0.0",
  "private": true,
  "description": "Shane's Blog",
  "author": "Shane",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build && gh-pages --dest /docs -d public -b main",
    "deploy-ci": "gatsby build --prefix-paths && gh-pages --dest /docs -d /public -r https://$GH_TOKEN@github.com/ShaneRich5/shanerich5.github.io.git"
  },
  "dependencies": {
    "@tailwindcss/aspect-ratio": "^0.2.1",
    "gatsby": "^3.6.2",
    "gatsby-plugin-manifest": "^3.6.0",
    "gatsby-plugin-sitemap": "^4.2.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "gatsby-plugin-postcss": "^4.6.0",
    "gh-pages": "^3.2.0",
    "postcss": "^8.3.0",
    "tailwindcss": "^2.1.4"
  }
}
